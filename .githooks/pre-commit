#!/bin/sh
# Install with: git config core.hooksPath .githooks
#
# `just quick` is the developer gate: type-check, format, unit tests, the
# layering gate and the conformance suite held to its ratchet.
set -e
exec just quick
